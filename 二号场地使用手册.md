# 二号场地使用手册

本文用于在二号场地的智元 X2 Ultra 真机上启动 RAICOM 2026 程序。文中状态与参数于
2026-08-11 在机器人上只读核对。

> 安全提示：涉及导航或肢体动作前，机器人必须双脚着地、周围没有人员和障碍物，
> 并由一人始终处于急停位置。未取得连续可靠的 SLAM 定位时不得启动导航。

## 1. 当前能力边界

| 功能 | 当前状态 | 是否可以启动 |
|---|---|---|
| 统一语音主循环 | 依赖和模型齐全 | 可以，但只能启动一份 |
| 时间、彩色数字识别、表情 | ROS 接口已接通 | 可以先做无运动测试 |
| 挥手、敬礼、双手交叉 | 接口已配置 | 仅在稳定站立和急停保护下测试 |
| Task1 导航 | 二号地图尚未完成重定位和坐标复核 | 不可以 |
| Task3 导航、对桌 | 桌子坐标仍为占位值 | 不可以 |
| Task3 抓取 | 深度、外参和夹爪未标定 | 不可以 |

`voice_run.sh` 是持续运行的语音总控，会按语音意图调度 Task1、Task2 和 Task3；
它不负责 SLAM 重定位，也不代表 Task3 抓取已经完成。

## 2. 从电脑连接机器人

在电脑终端执行：

```bash
ssh agi@192.168.31.25
```

首次连接出现主机指纹确认时，核对现场机器地址后输入 `yes`。随后在 SSH 的密码提示中
输入机器人密码；不要把密码写进脚本、本文或 Git 仓库。

登录成功后的提示符类似：

```text
agi@agi:~$
```

## 3. 进入部署目录并加载环境

```bash
cd /home/agi/x2_deploy_workspace/raicom_real_robot
pwd

source /opt/ros/humble/setup.bash
source /home/agi/aimdk/install/setup.bash
export ROS_DOMAIN_ID=0
export RMW_IMPLEMENTATION=rmw_fastrtps_cpp
```

`pwd` 必须输出：

```text
/home/agi/x2_deploy_workspace/raicom_real_robot
```

后续命令都在这个目录执行。

## 4. 防止重复启动主循环

启动前先检查：

```bash
pgrep -af '/home/agi/x2_deploy_workspace/raicom_real_robot/app/(speech_gateway|real_voice_controller)\.py'
```

如果同时看到 `speech_gateway.py` 和 `real_voice_controller.py`，说明统一语音主循环已经
运行。此时不要再次执行 `voice_run.sh`，否则两份控制器可能同时响应同一句口令。

需要停止时，应回到启动主循环的原终端按 `Ctrl+C`，等待两个进程都退出，再重新检查。
不要直接关闭终端窗口。

## 5. 检查配置与语音接口

先检查私有真机配置的结构，不打印配置内容：

```bash
export RAICOM_ROBOT_PROFILE=real
export PYTHONPATH="$PWD/app${PYTHONPATH:+:$PYTHONPATH}"
python3 app/profile_check.py
```

应看到：

```text
PASS profile=real_robot kind=real
ROS_DOMAIN_ID=0
RMW_IMPLEMENTATION=rmw_fastrtps_cpp
```

再执行语音接口预检：

```bash
./voice_preflight.sh
```

应看到：

```text
PASS 音频接口存在
```

仅在首次启用自研语音，或已经恢复官方普通交互模式时，执行下面的模式切换：

```bash
export RAICOM_CONFIRM_REAL_ROBOT=YES
./setup_only_voice.sh only_voice
./voice_preflight.sh
```

该操作会按官方流程重启 `agent`，不会重启 MC。执行前必须先停止已有的
`voice_run.sh`。

## 6. 启动统一语音主循环

只有第 4 节确认不存在旧进程，并且第 5 节通过后，才能执行：

```bash
./voice_run.sh
```

程序在前台持续运行，终端应出现类似日志：

```text
真机语音总控就绪
机器人回答: 我已经准备就绪，请说开始执行任务
```

保持这个终端开启。停止程序时在同一终端按 `Ctrl+C`。

## 7. 先做无运动语音测试

在尚未完成二号地图重定位前，只测试不会驱动机器人身体的口令：

```text
现在几点了
识别图片里的数字和颜色
做一个快乐表情
```

当前禁止使用以下导航口令：

```text
开始执行任务
去交互区
去作业区
返回出发区
```

挥手、敬礼和双手交叉会驱动上肢，只能在稳定站立且有人持急停时单项测试。

## 8. 二号地图与重定位

### 8.1 已核对的二号地图参数

机器人当前地图数据库只包含：

```text
地图名：2号
地图 ID：1786066723179
地图分辨率：20 像素/米
建图原点像素坐标：(465, 200)
地图图像尺寸：665 × 1176
```

可以用以下只读命令复核：

```bash
sqlite3 -readonly -header -column \
  /agibot/data/var/MapManagerModule/map.db \
  'SELECT map_id, map_name, map_info FROM map;'

sed -n '1,3p' \
  /agibot/data/var/MapManagerModule/1786066723179/grid_map_info.txt
```

### 8.2 不得直接运行官方示例

不要执行：

```bash
ros2 run py_examples relocate
```

当前机器人安装的示例把地图 ID 写死为 `1778133306741`，把初始像素位置写死为
`(1041, 630)`，与二号地图不匹配。

正确流程应使用经过现场验收的参数化重定位节点：

1. 把机器人放到二号地图建图原点，并核对真实朝向；
2. 向 `/integrated_command` 发布 `start_relocalization:1786066723179`；
3. 约一秒后向 `/relocalization_pose` 发布与真实摆放一致的像素位置和朝向；
4. 收到 `/slam/lidar_odom` 后才算重定位成功。

像素原点 `(465, 200)` 只能在机器人确实位于建图原点时使用；方向也必须现场确认，
不得只凭地图文件猜测。详见
[AimDK 官方 SLAM 文档](https://x2-aimdk.agibot.com/en/latest/Interface/perception/SLAM.html)。

重定位后进行只读验证：

```bash
timeout 10 ros2 topic hz /slam/lidar_odom
timeout 5 ros2 topic echo --once --qos-reliability best_effort /slam/lidar_odom
```

必须连续收到有限数值，并核对位置、朝向和机器人真实站位一致。定位无数据、跳变或坐标
不一致时，不得启动导航。

### 8.3 重新标定导航坐标

现有 `app/config/real_robot.json` 中的区域坐标来自旧的一号地图，不能直接用于二号地图。
完成重定位后，必须在同一地图坐标系重新测量并填写：

- 出发区、交互区 I、交互区 II 和作业区坐标；
- 出发朝向及交互区最终落脚偏置；
- 桌子物理坐标和对桌朝向；
- 真机速度、转向漂移补偿和位置容差。

现场私有配置不得提交 Git。每项参数都应低速、单项、重复测试后再用于比赛。

## 9. 导航验收完成后的启动方式

在 `/slam/lidar_odom` 连续有效、二号地图坐标已经复核且有人持急停时，使用以下两种
方式之一；不得同时运行。

### 9.1 语音统一入口

```bash
./voice_run.sh
```

听到准备就绪后说：

```text
开始执行任务
```

### 9.2 单任务调试入口

运行单任务前必须停止 `voice_run.sh`：

```bash
./run.sh 1
./run.sh 2
./run.sh 3 --navigate-and-align
```

- `./run.sh 1` 会执行 Task1 导航；
- `./run.sh 2` 进入交互式 Task2，输入 `quit` 退出；
- `./run.sh 3 --navigate-and-align` 会前往作业区并尝试对桌；当前桌子坐标未标定，
  因此暂不得执行；
- `3-grasp` 是受保护的接口调试入口，不是当前可用的完整抓取任务。

## 10. 当前已知问题

当前版本的 `preflight.py` 还有两处问题，因此不能把现有 `./preflight.sh` 的失败直接
理解为所有接口都不可用：

1. Task2 支持 `sensor_msgs/msg/CompressedImage`，但预检器把 RGB 类型写死为
   `sensor_msgs/msg/Image`；
2. 相机频率检查超过四秒时没有捕获 `TimeoutExpired`，会直接异常退出。

与此同时，以下问题是真实阻塞项：

- 当前没有 `/slam/lidar_odom` 定位输出；
- 二号地图区域坐标尚未重新标定；
- 深度图和深度相机内参不可用；
- `table_position_xy` 仍为占位值；
- `grasp.calibrated` 为 `false`。

## 11. 常用只读诊断

```bash
aima em doctor
ros2 node list | sort
ros2 node info /raicom_real_voice_controller
ros2 topic type /agent/process_audio_output
ros2 topic type /aima/hal/audio/playback
ros2 topic type /aima/hal/sensor/rgb_head_front_center/rgb_image/compressed
ros2 service type /aimdk_5Fmsgs/srv/SetMcAction
ros2 service type /aimdk_5Fmsgs/srv/SetMcInputSource
```

任何关键话题、服务或消息类型与本文不一致时，先停止比赛程序并重新核对固件和现场
配置，不要通过放宽安全检查强行启动。

# OmniPicker 双夹爪控制编程任务说明

## 一、任务目标

请在赛项提供的 `omnipicker_hand_student.py` 基础上补全程序，使其能够通过智元灵犀 X2 的官方 AimDK ROS 2 接口，分别控制左夹爪和右夹爪执行打开、闭合动作。

机器人运行模式、夹爪类型、设备参数及底层通信均已由赛项工作人员提前配置，不属于本任务的考察内容。参赛学生只需完成 Python 程序中的夹爪命令构造与发布逻辑，不得修改机器人底层配置，也不得直接操作 CAN 或 EtherCAT。

## 二、提供的文件与参考资料

赛项提供：

```text
omnipicker_hand_student.py
```

官方参考资料：

<https://x2-aimdk.agibot.com/zh-cn/latest/example/Python.html#py-hand-control>

请重点阅读官方页面中的 Python 夹爪控制示例，并结合脚本内的 TODO 注释完成程序。

## 三、已完成的赛前配置

赛项工作人员已完成以下内容：

- 机器人与左右夹爪的安装和通信配置；
- 左右手设备类型配置；
- 夹爪固件及设备参数配置；
- ROS 2 与 AimDK 运行环境配置；
- SoC0、SoC1 之间的夹爪控制通信链路配置；
- 左右夹爪与机器人手臂的对应关系确认。

参赛学生不得更改上述配置，也不需要执行停止机器人运控模块、修改手型配置、修改夹爪 ID、刷新固件等操作。

## 四、程序接口约定

程序使用以下官方 ROS 2 控制话题：

```text
/aima/hal/joint/hand/command
```

需要使用的主要消息类型包括：

```python
HandCommandArray
HandCommand
HandType
MessageHeader
```

夹爪位置采用归一化数值：

| 位置值 | 动作含义 |
|---:|---|
| `0.0` | 闭合 |
| `1.0` | 打开 |

本任务只要求实现左、右夹爪的完全打开和完全闭合，不要求实现任意位置控制或双夹爪同时控制。

## 五、需要完成的编程内容

脚本中共有三处核心 TODO。

### TODO 1：创建单侧夹爪命令

完成 `create_hand_command()` 函数：

- 创建一条 `HandCommand`；
- 设置逻辑关节名称；
- 设置目标位置；
- 设置速度、加速度、减速度和力参数；
- 返回填写完成的消息。

字段名称和基本写法可参考官方示例。左右夹爪的逻辑关节名称已在脚本顶部给出，按目标侧选用即可。

### TODO 2：组装左右手控制消息

完成 `build_hand_message()` 函数：

- 创建 `HandCommandArray`；
- 填写消息头；
- 根据 `hand` 参数选择左夹爪或右夹爪；
- 将目标侧声明为夹爪类型并填入一条控制命令；
- 非目标侧设置为无设备类型，命令列表为空；
- 返回完整消息。

单侧控制时，不得向另一侧发送位置目标。

### TODO 3：持续发布控制命令

完成 `publish_command()` 方法：

- 调用 `build_hand_message()` 构造消息；
- 按脚本中指定的频率持续发布；
- 按脚本中指定的时长结束发布；
- 循环期间保持 ROS 2 节点正常处理事件；
- 输出实际发布帧数。

不得将控制逻辑改为只发布一帧。

## 六、运行命令

程序补全并通过检查后，使用以下命令运行。

打开左夹爪：

```bash
python3 omnipicker_hand_student.py --publish open left
```

闭合左夹爪：

```bash
python3 omnipicker_hand_student.py --publish close left
```

打开右夹爪：

```bash
python3 omnipicker_hand_student.py --publish open right
```

闭合右夹爪：

```bash
python3 omnipicker_hand_student.py --publish close right
```

不带 `--publish` 时，程序不得向机器人发布控制命令。例如：

```bash
python3 omnipicker_hand_student.py open left
```

该命令仅用于确认参数解析和程序启动是否正常。

## 七、完成要求

完成后的程序应满足以下要求：

1. 四条规定命令均可正常执行；
2. `open` 对应目标位置 `1.0`，`close` 对应目标位置 `0.0`；
3. 控制左夹爪时，右夹爪不发生非预期动作；
4. 控制右夹爪时，左夹爪不发生非预期动作；
5. 使用官方 `HandCommandArray` 消息和指定 ROS 2 话题；
6. 按规定频率和时长持续发布命令；
7. 不直接操作 CAN、EtherCAT，不修改机器人及夹爪配置；
8. 程序结构清晰，能够正常响应 `Ctrl+C` 并退出。

## 八、建议调试顺序

建议依次进行：

1. 阅读脚本中的三个 TODO；
2. 对照官方夹爪控制示例确认消息字段；
3. 先完成消息构造，再完成发布循环；
4. 不带 `--publish` 运行，检查参数解析与依赖导入；
5. 由现场指导人员确认安全后，先测试单侧打开；
6. 再依次测试同侧闭合和另一侧动作；
7. 检查是否只控制了命令指定的一侧。

## 九、安全要求

> **严禁带电拔插夹爪、线缆或机器人接口。**

实机运行前必须遵守以下要求：

- 服从现场指导人员的统一安排；
- 确认机器人处于稳定、安全的工作状态；
- 确认夹爪运动范围内无人手、线缆、工具及易损物；
- 首次发布命令前，应由现场指导人员确认程序和目标侧；
- 不得同时运行多个夹爪控制程序；
- 发现左右夹爪对应错误、非目标侧动作或异常声音时，应立即停止程序并报告；
- 不得自行停止或重启机器人运控模块；
- 不得自行修改手型、CAN ID、通信参数、固件或机器人系统文件。

## 十、提交内容

提交补全后的：

```text
omnipicker_hand_student.py
```

程序应保留原有命令行形式和安全开关，不得通过绕过 TODO、调用外部完整控制脚本等方式完成任务。

# 2026-08-11 X2 真机现场调试报告

## 保存位置与备份状态

本次真机代码已同时保存在以下两处：

- 本地比赛仓库：`competition/real_robot/`
- X2 机器人：`/home/agi/x2_deploy_workspace/raicom_real_robot/`

核心代码在两端的 SHA-256 已核对一致，包括 Task1、Task2、Task3、语音总控、
表情执行器、预设动作执行器和统一启动脚本。

现场私有配置也已从机器人备份到本地：

- 本地：`competition/real_robot/app/config/real_robot.json`
- 真机：`/home/agi/x2_deploy_workspace/raicom_real_robot/app/config/real_robot.json`

该文件由 `.gitignore` 排除，避免 API Key、现场参数等私有信息进入 GitHub。
可提交和分享的是 `real_robot.template.json`。

## 今日完成的真机工作

### 1. 真机独立部署

- 所有自研代码放入 `/home/agi/x2_deploy_workspace/raicom_real_robot/`。
- 未修改 AimDK 官方目录和官方代码。
- 使用 ROS Domain 0 和 `rmw_fastrtps_cpp`。
- 统一入口为 `run.sh`、`voice_run.sh` 和 `app/competition_run.sh`。

### 2. Task1 导航现场适配

- 接入真机 SLAM 位姿和官方地图。
- 对出发区至交互区 I 的前进、转向、横移补偿和倒退距离进行了现场调整。
- 记录官方地图 `1号`，地图 ID `1786066224954`。
- 记录作业区标准站位：`x=-0.298 m, y=-2.559 m, yaw=-98.6°`。
- 详细坐标见 `calibration/site_2026-08-11.md`。

注意：Task1 已能现场行走，但最终落脚位置仍需继续做重复性测试，不能仅凭单次成功
认定比赛稳定。

### 3. 自研语音链路

- 使用 X2 麦克风、SenseVoice 离线 ASR、规则语义和 DeepSeek 需求判定。
- 使用自研 PCM 播放链路，未调用组委会禁止的 `PlayTts`。
- 已接入真机扬声器并把播放增益设为 2.0。
- 未知环境语音只写日志，不再反复播报“没有听懂”。
- DeepSeek 只允许辅助 Task3 养老需求分类，不能把环境语音直接变成导航或动作。

### 4. Task2 基础交互

- 时间询问可以使用本地时间回答。
- 表情改为官方 `/face_ui_proxy/play_emoji` 服务。
- 发现内置交互占用屏幕优先级 60，现使用优先级 100；快乐、愤怒、睡觉已现场成功。
- 摄像头改用头部中央相机压缩图话题，已经能收到并解码真机 JPEG 图像。
- 彩色数字识别链路已经接通，但现场卡片数字识别准确率仍未最终验收。
- 左右挥手、左右敬礼、双手交叉的指令映射已经配置。
- 修复预设动作前错误报告“未站稳”的原因：跨板 `SetMcAction` 调用超时。
- 新增 `motion_player.py`，按照 AimDK 官方示例更新时间戳并进行 8 次快速重试。

动作重试修复已经部署，但尚未进行修复后的第一次实际挥手验收。

### 5. Task3

- 已保留语音需求识别、导航到作业区和对桌逻辑。
- 作业区站位已记录。
- 抓取仍未达到比赛可用状态；真机 RGB-D、桌面物体坐标、相机外参、机械臂 IK、
  手指闭合和抓取成功判断仍需现场标定与验证。

## 当前重要文件

- `app/task1_agent.py`：Task1 真机导航。
- `app/task2_agent.py`：Task2 时间、视觉、表情和预设动作。
- `app/task3_scene_agent.py`：Task3 需求判断、导航和对桌。
- `app/real_voice_controller.py`：语音总控和任务调度。
- `app/speech_gateway.py`：自研语音播放网关。
- `app/emoji_player.py`：官方表情服务同步执行器。
- `app/motion_player.py`：SD 切换与预设动作同步重试执行器。
- `app/task2_vision.py`：彩色数字识别。
- `app/config/real_robot.json`：现场私有配置，不提交 Git。
- `calibration/site_2026-08-11.md`：现场地图和作业区坐标。

## 下次调试优先顺序

1. 机器人站稳且有人持急停时，说“挥挥左手”，验证新的动作执行器。
2. 分别验证左右挥手、左右敬礼、双手交叉，每项至少 3 次。
3. 从标准出发点重复 Task1 至少 5 次，记录双脚是否完整进入交互区 I。
4. 用正式比赛数字卡验证头部相机取图、颜色和数字识别。
5. 单独标定桌子物理位置与相机外参，然后再进行 Task3 机械臂测试。

## Git 状态说明

代码已经保存在本地磁盘和机器人中，但当前大量真机目录文件仍是 Git 未跟踪状态，
尚未形成新的提交，也尚未推送到 GitHub。提交前必须先检查私有配置和 API Key，
确认它们没有进入暂存区。
